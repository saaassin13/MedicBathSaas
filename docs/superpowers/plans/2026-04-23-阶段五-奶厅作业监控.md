# 阶段五：奶厅作业监控页

**目标**：实现奶厅作业监控页面，包含4×3监控网格、状态标签、奶厅筛选

**关联**：
- 前置：阶段二（布局组件）
- 后续：其他页面

**验收**：
- 监控网格4×3布局正常
- 12个监控卡片正常显示
- 状态标签（运行正常/异常中断/未启动）正确显示
- 位置信息正常显示
- 奶厅筛选正常

---

## 任务 5.1：创建监控卡片组件

**Files:**
- Create: `src/pages/Monitoring/MonitorCard.tsx`
- Create: `src/pages/Monitoring/Monitoring.module.css`

**目标**：创建单个监控卡片组件，包含视频区、状态标签、位置信息

**测试**：
- 验证状态标签颜色正确
- 验证位置信息显示正确
- 验证无信号状态显示

- [ ] **Step 1: 创建 Monitoring.module.css**

```css
.monitoring-page {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.page-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.hall-selector {
  width: 200px;
}

.monitor-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 16px;
}

.monitor-card {
  position: relative;
  border-radius: 8px;
  overflow: hidden;
  background: #1a1a1a;
  aspect-ratio: 16/9;
}

.monitor-card video,
.monitor-card .placeholder {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.status-badge {
  position: absolute;
  top: 8px;
  left: 8px;
  padding: 4px 8px;
  border-radius: 4px;
  font-size: 12px;
  display: flex;
  align-items: center;
  gap: 4px;
}

.status-badge.normal {
  background: #e6f7ff;
  color: #52c41a;
}

.status-badge.error {
  background: #fff2e8;
  color: #ff4d4f;
}

.status-badge.stopped {
  background: #f5f5f5;
  color: #8c8c8c;
}

.status-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  animation: pulse 2s infinite;
}

.status-dot.normal { background: #52c41a; }
.status-dot.error { background: #ff4d4f; }
.status-dot.stopped { background: #8c8c8c; }

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

.location-bar {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 8px 12px;
  background: rgba(255, 140, 0, 0.8);
  color: #fff;
  font-size: 12px;
}

.no-signal {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
  color: #666;
  font-size: 14px;
}
```

- [ ] **Step 2: 创建 MonitorCard.tsx**

```typescript
import { MonitorCard as MonitorCardType } from '../../types'
import styles from './Monitoring.module.css'

interface MonitorCardProps {
  data: MonitorCardType
}

export default function MonitorCard({ data }: MonitorCardProps) {
  const getStatusClass = (status: string) => {
    switch (status) {
      case 'normal': return styles.normal
      case 'error': return styles.error
      case 'stopped': return styles.stopped
      default: return ''
    }
  }

  const getStatusText = (status: string) => {
    switch (status) {
      case 'normal': return '运行正常'
      case 'error': return '异常中断'
      case 'stopped': return '未启动'
      default: return ''
    }
  }

  const locationText = [
    data.location.group,
    data.location.region,
    data.location.farm,
    `${data.location.milkingHall}-${data.location.equipment}`,
    data.location.process,
    data.location.viewType,
  ].join('-')

  return (
    <div className={styles['monitor-card']}>
      {data.status === 'stopped' ? (
        <div className={styles['no-signal']}>无信号</div>
      ) : (
        <div className={styles.placeholder}>
          {/* 后续接入实时视频 */}
        </div>
      )}
      <div className={`${styles['status-badge']} ${getStatusClass(data.status)}`}>
        <span className={`${styles['status-dot']} ${getStatusClass(data.status).split(' ')[0]}`} />
        {getStatusText(data.status)}
      </div>
      <div className={styles['location-bar']}>
        {locationText}
      </div>
    </div>
  )
}
```

- [ ] **Step 3: 提交**

```bash
git add src/pages/Monitoring/MonitorCard.tsx src/pages/Monitoring/Monitoring.module.css
git commit -m "feat: 创建监控卡片组件
- 状态标签（运行正常/异常中断/未启动）
- 位置信息浮层
- 无信号占位图"
```

---

## 任务 5.2：创建监控网格组件

**Files:**
- Create: `src/pages/Monitoring/MonitorGrid.tsx`

**目标**：创建4×3监控网格容器

**测试**：
- 验证网格4列布局
- 验证12个卡片正常显示

- [ ] **Step 1: 创建 MonitorGrid.tsx**

```typescript
import MonitorCard from './MonitorCard'
import { MonitorCard as MonitorCardType } from '../../types'
import styles from './Monitoring.module.css'

interface MonitorGridProps {
  cards: MonitorCardType[]
}

export default function MonitorGrid({ cards }: MonitorGridProps) {
  return (
    <div className={styles['monitor-grid']}>
      {cards.map((card) => (
        <MonitorCard key={card.id} data={card} />
      ))}
    </div>
  )
}
```

- [ ] **Step 2: 提交**

```bash
git add src/pages/Monitoring/MonitorGrid.tsx
git commit -m "feat: 创建监控网格组件
- 4×3 网格布局
- 渲染12个监控卡片"
```

---

## 任务 5.3：创建监控页面主组件

**Files:**
- Create: `src/pages/Monitoring/MonitoringPage.tsx`
- Modify: `src/App.tsx`（更新路由）

**目标**：整合奶厅筛选和监控网格

**测试**：
- 验证奶厅筛选下拉正常
- 验证面包屑显示正确

- [ ] **Step 1: 创建 MonitoringPage.tsx**

```typescript
import { useState } from 'react'
import { Select, Empty } from 'antd'
import MonitorGrid from './MonitorGrid'
import { MonitorCard } from '../../types'
import styles from './Monitoring.module.css'

// 模拟12个监控卡片数据（包含1期和2期奶厅）
const mockMonitorCards: MonitorCard[] = Array.from({ length: 12 }, (_, i) => ({
  id: `monitor-${i}`,
  location: {
    group: '蒙牛集团',
    region: '东北大区',
    farm: '双城',
    milkingHall: i < 6 ? '1期奶厅' : '2期奶厅',  // 前6个是1期，后6个是2期
    equipment: `${i % 4 + 1}号转盘`,
    process: i % 2 === 0 ? '前药浴' : '后药浴',
    viewType: i % 2 === 0 ? '监控视角' : '作业视角',
  },
  status: i === 3 ? 'error' : i === 7 ? 'stopped' : 'normal',
  thumbnail: '',
}))

const hallOptions = [
  { value: 'all', label: '全部奶厅' },
  { value: '1期奶厅', label: '1期奶厅' },
  { value: '2期奶厅', label: '2期奶厅' },
]

export default function MonitoringPage() {
  const [selectedHall, setSelectedHall] = useState('all')

  // 根据选中奶厅过滤监控卡片
  const filteredCards = mockMonitorCards.filter(card =>
    selectedHall === 'all' || card.location.milkingHall === selectedHall
  )

  return (
    <div className={styles['monitoring-page']}>
      <div className={styles['page-header']}>
        <h2>奶厅作业监控</h2>
        <Select
          className={styles['hall-selector']}
          value={selectedHall}
          onChange={setSelectedHall}
          options={hallOptions}
        />
      </div>
      {filteredCards.length > 0 ? (
        <MonitorGrid cards={filteredCards} />
      ) : (
        <Empty description="暂无监控数据" />
      )}
    </div>
  )
}
```

- [ ] **Step 2: 更新 App.tsx 添加路由**

```typescript
import MonitoringPage from './pages/Monitoring/MonitoringPage'

<Route
  path="/monitoring"
  element={
    <MainLayout>
      <MonitoringPage />
    </MainLayout>
  }
/>
```

- [ ] **Step 3: 验证**

Run: `npm run dev`
Expected: 访问 /monitoring 显示监控页面，4×3网格

- [ ] **Step 4: 提交**

```bash
git add src/pages/Monitoring/MonitoringPage.tsx src/App.tsx
git commit -m "feat: 创建奶厅作业监控页面
- 奶厅筛选下拉
- 4×3 监控网格
- 状态标签和位置信息"
```

---

## 阶段五验收

- [ ] 页面显示面包屑"奶厅作业监控"
- [ ] 奶厅筛选下拉正常
- [ ] 监控网格4×3布局
- [ ] 12个监控卡片正常显示
- [ ] 状态标签正确（运行正常绿色、异常红色、未启动灰色）
- [ ] 位置信息正确显示
- [ ] 无信号卡片显示"无信号"

---

## 下一步

进入阶段六：维保管理模块