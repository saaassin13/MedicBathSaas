# 阶段三：登录页

**目标**：实现登录页面，包含毛玻璃效果背景、用户名/密码输入框、登录按钮

**关联**：
- 前置：阶段一（项目基础结构）+ 阶段二（布局组件）
- 后续：登录成功后跳转用户管理页

**验收**：
- 页面显示全屏背景图
- 登录卡片毛玻璃效果正常
- 用户名/密码输入框正常
- 点击登录按钮显示验证错误或跳转

---

## 任务 3.1：创建登录页样式

**Files:**
- Create: `src/pages/Login/Login.module.css`

**目标**：定义登录页样式，包含背景、卡片、输入框、按钮样式

- [ ] **Step 1: 创建 Login.module.css**

```css
/* 全屏背景容器 */
.login-container {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: url('/assets/login-bg.jpg') center/cover no-repeat;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  padding-right: 15%;
}

/* 毛玻璃登录卡片 */
.login-card {
  width: 400px;
  padding: 40px;
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(20px);
  border-radius: 16px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
}

/* 系统标题 */
.login-title {
  font-size: 24px;
  font-weight: 700;
  color: #333;
  text-align: center;
  margin-bottom: 32px;
}

/* 表单样式 */
.login-form {
  display: flex;
  flex-direction: column;
  gap: 24px;
}

/* 输入框容器 */
.input-wrapper {
  position: relative;
}

.input-icon {
  position: absolute;
  left: 16px;
  top: 50%;
  transform: translateY(-50%);
  color: #999;
}

.input-field {
  width: 100%;
  height: 48px;
  padding: 0 16px 0 48px;
  border: 1px solid #e0e0e0;
  border-radius: 8px;
  font-size: 16px;
  transition: border-color 0.3s;
}

.input-field:focus {
  outline: none;
  border-color: #ff7300;
}

/* 登录按钮 */
.login-button {
  width: 100%;
  height: 48px;
  background: #ff8c00;
  color: #fff;
  border: none;
  border-radius: 8px;
  font-size: 16px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.3s;
}

.login-button:hover {
  background: #e66600;
}

.login-button:disabled {
  background: #d9d9d9;
  cursor: not-allowed;
}

/* 底部提示 */
.login-hint {
  position: fixed;
  bottom: 24px;
  left: 50%;
  transform: translateX(-50%);
  font-size: 12px;
  color: rgba(255, 255, 255, 0.8);
  text-shadow: 0 1px 2px rgba(0, 0, 0, 0.5);
}

/* 错误提示 */
.error-text {
  color: #ff4d4f;
  font-size: 12px;
  margin-top: 4px;
}

/* Logo 样式 */
.login-logo {
  position: fixed;
  top: 24px;
  left: 24px;
  display: flex;
  align-items: center;
  gap: 12px;
}

.login-logo-icon {
  width: 40px;
  height: 40px;
  background: #ff8c00;
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-size: 20px;
  font-weight: bold;
}

.login-logo-text {
  color: #fff;
  font-size: 16px;
  font-weight: 500;
  text-shadow: 0 1px 4px rgba(0, 0, 0, 0.3);
}
```

- [ ] **Step 2: 提交**

```bash
git add src/pages/Login/Login.module.css
git commit -m "chore: 创建登录页样式文件"
```

---

## 任务 3.2：创建 FormInput 组件

**Files:**
- Create: `src/components/common/FormInput.tsx`
- Create: `src/components/common/FormInput.module.css`

**目标**：创建可复用的带图标输入框组件

**测试**：
- 验证输入框显示图标
- 验证聚焦状态样式
- 验证错误状态样式

- [ ] **Step 1: 创建 FormInput.module.css**

```css
/* 输入框容器 */
.input-wrapper {
  position: relative;
}

.input-icon {
  position: absolute;
  left: 16px;
  top: 50%;
  transform: translateY(-50%);
  color: #999;
}

.input-field {
  width: 100%;
  height: 48px;
  padding: 0 16px 0 48px;
  border: 1px solid #e0e0e0;
  border-radius: 8px;
  font-size: 16px;
  transition: border-color 0.3s;
}

.input-field:focus {
  outline: none;
  border-color: #ff7300;
}

/* 错误提示 */
.error-text {
  color: #ff4d4f;
  font-size: 12px;
  margin-top: 4px;
}
```

- [ ] **Step 2: 创建 FormInput.tsx**

```typescript
import { UserOutlined, LockOutlined } from '@ant-design/icons'
import styles from './FormInput.module.css'

interface FormInputProps {
  type: 'text' | 'password'
  placeholder: string
  value: string
  onChange: (value: string) => void
  error?: string
  prefixIcon?: React.ReactNode
}

export default function FormInput({
  type,
  placeholder,
  value,
  onChange,
  error,
  prefixIcon,
}: FormInputProps) {
  const Icon = type === 'password' ? LockOutlined : UserOutlined

  return (
    <div>
      <div className="input-wrapper">
        <span className="input-icon">
          {prefixIcon || <Icon />}
        </span>
        <input
          type={type}
          className="input-field"
          placeholder={placeholder}
          value={value}
          onChange={(e) => onChange(e.target.value)}
          style={error ? { borderColor: '#ff4d4f' } : {}}
        />
      </div>
      {error && <div className="error-text">{error}</div>}
    </div>
  )
}
```

- [ ] **Step 3: 提交**

```bash
git add src/components/common/FormInput.tsx
git commit -m "feat: 创建 FormInput 通用组件
- 支持文本/密码类型
- 支持图标显示
- 支持错误状态"
```

---

## 任务 3.3：创建 LoginForm 组件

**Files:**
- Create: `src/pages/Login/LoginForm.tsx`

**目标**：登录表单逻辑，包含验证和提交

**测试**：
- 验证空用户名显示"请输入用户名"
- 验证空密码显示"请输入密码"
- 验证用户名字数<3显示错误
- 验证密码<6位显示错误
- 验证登录成功后跳转

- [ ] **Step 1: 创建 LoginForm.tsx**

```typescript
import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import FormInput from '../../components/common/FormInput'
import styles from './Login.module.css'

interface FormErrors {
  username?: string
  password?: string
}

export default function LoginForm() {
  const navigate = useNavigate()
  const [username, setUsername] = useState('')
  const [password, setPassword] = useState('')
  const [errors, setErrors] = useState<FormErrors>({})
  const [loading, setLoading] = useState(false)

  const validate = (): boolean => {
    const newErrors: FormErrors = {}

    if (!username.trim()) {
      newErrors.username = '请输入用户名'
    } else if (username.length < 3) {
      newErrors.username = '用户名至少3个字符'
    }

    if (!password.trim()) {
      newErrors.password = '请输入密码'
    } else if (password.length < 6) {
      newErrors.password = '密码至少6个字符'
    }

    setErrors(newErrors)
    return Object.keys(newErrors).length === 0
  }

  const handleSubmit = async () => {
    if (!validate()) return

    setLoading(true)
    // 模拟登录请求
    setTimeout(() => {
      setLoading(false)
      // 登录成功，跳转到用户管理页
      navigate('/user-management')
    }, 1000)
  }

  const handleKeyDown = (e: React.KeyboardEvent) => {
    if (e.key === 'Enter') {
      handleSubmit()
    }
  }

  return (
    <div className="login-card">
      <h1 className="login-title">奶牛药浴大数据管理中心</h1>
      <div className="login-form" onKeyDown={handleKeyDown}>
        <FormInput
          type="text"
          placeholder="用户名"
          value={username}
          onChange={setUsername}
          error={errors.username}
        />
        <FormInput
          type="password"
          placeholder="密码"
          value={password}
          onChange={setPassword}
          error={errors.password}
        />
        <button
          className="login-button"
          onClick={handleSubmit}
          disabled={loading}
        >
          {loading ? '登录中...' : '登录'}
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: 提交**

```bash
git add src/pages/Login/LoginForm.tsx
git commit -m "feat: 创建 LoginForm 组件
- 表单验证逻辑
- 登录按钮加载状态
- 回车键提交支持"
```

---

## 任务 3.4：创建 LoginPage 主组件

**Files:**
- Create: `src/pages/Login/LoginPage.tsx`
- Modify: `src/App.tsx`（更新登录页路由）

**目标**：整合登录页所有组件

**测试**：
- 验证全屏背景正常显示
- 验证毛玻璃卡片正常显示
- 验证表单提交后跳转

- [ ] **Step 1: 创建 LoginPage.tsx**

```typescript
import LoginForm from './LoginForm'
import styles from './Login.module.css'

export default function LoginPage() {
  return (
    <>
      <div className="login-logo">
        <div className="login-logo-icon">S</div>
        <span className="login-logo-text">Scaling Robotics</span>
      </div>
      <div className="login-container">
        <LoginForm />
      </div>
      <div className="login-hint">
        建议使用Chrome浏览器，在1920*1080分辨率下使用效果最佳
      </div>
    </>
  )
}
```

- [ ] **Step 2: 更新 App.tsx**

```typescript
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom'
import MainLayout from './components/Layout/MainLayout'
import LoginPage from './pages/Login/LoginPage'

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Navigate to="/login" replace />} />
        <Route path="/login" element={<LoginPage />} />
        <Route
          path="/monitoring"
          element={
            <MainLayout>
              <div>奶厅作业监控占位</div>
            </MainLayout>
          }
        />
        {/* ... 其他路由保持不变 ... */}
      </Routes>
    </BrowserRouter>
  )
}

export default App
```

- [ ] **Step 3: 添加背景图片占位**

创建 `public/assets/login-bg.jpg` 作为占位背景图

- [ ] **Step 4: 验证**

Run: `npm run dev`
Expected: 访问 /login 显示登录页，毛玻璃效果，表单可提交

- [ ] **Step 5: 提交**

```bash
git add src/pages/Login/LoginPage.tsx src/App.tsx
git commit -m "feat: 创建登录页主组件
- 整合 Logo、表单、背景
- 更新路由配置
- 添加背景图片占位"
```

---

## 任务 3.5：创建登录页单元测试

**Files:**
- Create: `src/pages/Login/LoginForm.test.tsx`

**目标**：验证登录表单验证逻辑

**测试场景**：
- 空表单提交显示错误
- 用户名少于3字符显示错误
- 密码少于6字符显示错误
- 有效输入无错误显示

- [ ] **Step 1: 创建测试文件**

```typescript
import { render, screen, fireEvent } from '@testing-library/react'
import LoginForm from './LoginForm'

describe('LoginForm', () => {
  it('显示空用户名错误', () => {
    render(<LoginForm />)
    fireEvent.click(screen.getByText('登录'))
    expect(screen.getByText('请输入用户名')).toBeInTheDocument()
  })

  it('显示空密码错误', () => {
    render(<LoginForm />)
    fireEvent.change(screen.getByPlaceholderText('用户名'), {
      target: { value: 'admin' },
    })
    fireEvent.click(screen.getByText('登录'))
    expect(screen.getByText('请输入密码')).toBeInTheDocument()
  })

  it('用户名字数少于3显示错误', () => {
    render(<LoginForm />)
    fireEvent.change(screen.getByPlaceholderText('用户名'), {
      target: { value: 'ab' },
    })
    fireEvent.click(screen.getByText('登录'))
    expect(screen.getByText('用户名至少3个字符')).toBeInTheDocument()
  })

  it('密码少于6位显示错误', () => {
    render(<LoginForm />)
    fireEvent.change(screen.getByPlaceholderText('用户名'), {
      target: { value: 'admin' },
    })
    fireEvent.change(screen.getByPlaceholderText('密码'), {
      target: { value: '12345' },
    })
    fireEvent.click(screen.getByText('登录'))
    expect(screen.getByText('密码至少6个字符')).toBeInTheDocument()
  })

  it('有效输入无错误显示', () => {
    render(<LoginForm />)
    fireEvent.change(screen.getByPlaceholderText('用户名'), {
      target: { value: 'admin' },
    })
    fireEvent.change(screen.getByPlaceholderText('密码'), {
      target: { value: 'password123' },
    })
    fireEvent.click(screen.getByText('登录'))
    expect(screen.queryByText('请输入用户名')).not.toBeInTheDocument()
    expect(screen.queryByText('请输入密码')).not.toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 运行测试**

Run: `npm test`
Expected: 所有测试通过

- [ ] **Step 3: 提交**

```bash
git add src/pages/Login/LoginForm.test.tsx
git commit -m "test: 添加登录表单单元测试
- 验证空表单提交错误
- 验证用户名格式错误
- 验证密码格式错误
- 验证有效输入无错误"
```

---

## 阶段三验收

- [ ] 登录页背景正常显示（使用占位图）
- [ ] 毛玻璃登录卡片正常显示
- [ ] Logo显示在左上角
- [ ] 用户名输入框正常
- [ ] 密码输入框正常
- [ ] 空用户名提交显示"请输入用户名"
- [ ] 空密码提交显示"请输入密码"
- [ ] 用户名<3显示"用户名至少3个字符"
- [ ] 密码<6显示"密码至少6个字符"
- [ ] 登录成功后跳转 /user-management
- [ ] 底部提示文字显示正确
- [ ] 单元测试全部通过

---

## 下一步

进入阶段四：用户管理模块